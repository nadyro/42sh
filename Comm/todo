antoipom
leaks!
historique multiligne

armon:
-EXEC/PARSER:
	-manage leaks
	-handle redirections
	-handle pipes (maybe Thibaud can if he's already done it for 21sh?)
	-handle heredoc
	-norm
-BUILTINS:
	-need to fix bugs with env <utility_call> (env -i <utility_call> seems to work)
	-handle exit builtin
	-go through all builtins with parser data structures and verify compatibility
	
nadir:
-LINE EDITING:
	-Auto completion v1 to finish. (On branch autocompletion lel)
